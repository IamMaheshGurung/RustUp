[package]
name = "salary_tracker_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
dotenvy = "0.15"
chrono = "0.4"

sqlx = { version = "0.6", features = ["postgres", "runtime-tokio-native-tls"] }


